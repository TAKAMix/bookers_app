<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="stylesheets/books.scss" />
    <title>WebCampBackendNew</title>
  </head>
  
  <body>
    <h1>Books</h1>
    <table>
      <thead>
        <tr>
        <th>Title</th>
        <th>body</th>
        <th colspan="3"></th>
        </tr>
      </thead>
      
      <tbody>
        <tr>
          <% @books.each do |book| %>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
          <td><%= link_to "Show", book_path(book.id) %></td>
          <td><%= link_to "Edit", edit_book_path(book) %></td>
          <td><%= link_to "Destroy", destroy_book_path(book.id), method: :delete, "data-confirm" => "Are You sure？" %></td>
        </tr>         
          <% end %>
      </tbody>
    </table>
    
    <h2>New book</h2>
    
    <!--バリデーションの実装-->
  <div id="error_explanation">
    <% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  </div>
  <% end %>
    
      <%= form_with model: @book do |f| %>
    <div class="field">
      <label>Title</label>
      <label><%= f.text_field :title %></label>
    </div>
    
    <div class="field">
      <label>Body</label>
      <label><%= f.text_area :body %></label>
    </div>
    <div class="actions">
      <label><%= f.submit 'Create Book' %></label>
    </div>
	 <% end %>
	 
	</body>
</html>